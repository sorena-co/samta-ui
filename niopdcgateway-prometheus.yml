apiVersion: monitoring.coreos.com/v1alpha1
kind: ServiceMonitor
metadata:
  name: niopdcgateway-app
  namespace: default
  labels:
    team: default
spec:
  selector:
    matchLabels:
      app: niopdcgateway
  endpoints:
  - port: web
    path: /prometheusMetrics
